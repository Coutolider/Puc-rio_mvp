3. Modelagem

Foi adotado um modelo dimensional baseado em Esquema Estrela, contendo:

Tabela Fato:

FATO_SUICIDIOS: representa os registros filtrados de óbitos por suicídio (CID-10 entre X60 e X84).

Tabelas Dimensão:

DIM_SEXO

DIM_RACACOR

DIM_ESTCIV

DIM_ESC

DIM_LOCOCOR

DIM_ASSISTMED

DIM_UF

Cada dimensão codifica e descreve os atributos categóricos presentes na fato.

Catálogo de Dados

Um catálogo contendo a descrição dos campos foi elaborado, incluindo domínio de valores e explicação de campos calculados, como:

DATA_OBITO (convertido de DTOBITO)

HORA_OBITO (extraído de HORAOBITO)

FAIXA_ETARIA (calculada a partir de IDADE)

ANO, MES (extraídos de DATA_OBITO)
